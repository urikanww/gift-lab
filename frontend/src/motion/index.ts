/**
 * Motion foundation — public API.
 *
 * Import shared presets and helpers from here so animation stays consistent
 * across the app instead of one-off tweaks per component:
 *
 *   import { fadeInUp, staggerContainer, useReducedMotionSafe } from '@/motion';
 */
export * from './transitions';
export * from './variants';
export * from './reducedMotion';
export * from './Motion';
export * from './PageTransition';
